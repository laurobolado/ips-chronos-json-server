{
  "name": "ips-chronos-json-server",
  "version": "1.0.0",
  "description": "IPS Chronos Mock JSON Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json"
  },
  "author": "Lauro Bolado",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
